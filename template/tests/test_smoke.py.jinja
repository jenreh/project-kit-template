"""Smoke test so a freshly generated project has a green `task test`.

Replace with real tests as the project grows.
"""

import {{ package_name }}


def test_version() -> None:
    assert {{ package_name }}.__version__
